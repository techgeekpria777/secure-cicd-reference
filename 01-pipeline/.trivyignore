# Risk-accepted starlette findings. Reviewed 2026-07-10. Next review 2026-10-01.
# Service runs on Linux (python:3.12-slim), uses only JSON API routes:
# no StaticFiles, no FileResponse, no path-based auth middleware.
# No fixed version reachable from current dependency set (FastAPI 0.124.4 caps starlette <0.51.0).

# StaticFiles UNC/SSRF on Windows. Not applicable: Linux runtime, no StaticFiles.
CVE-2026-48818

# FileResponse/Range DoS. Not applicable: service serves no files via FileResponse/StaticFiles.
CVE-2025-62727